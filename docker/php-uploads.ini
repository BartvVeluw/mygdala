; Base php:8.2-apache image defaults to upload_max_filesize=2M / post_max_size=8M,
; too small for a form upload field's 10 MB ceiling (see
; App\Service\Forms\FormFileTypes) and the Homepage Hero's 30 MB video
; upload limit (see App\Service\SectionVideoUploader). On production (Vimexx
; shared hosting, no Docker) the equivalent limits must be raised via
; .user.ini or the hosting control panel — see MAIN.MD.
upload_max_filesize = 35M
post_max_size = 40M
